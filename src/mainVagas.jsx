import React from "react";
import ReactDOM from "react-dom/client";
import PublicVagas from "./PublicVagas.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PublicVagas />
  </React.StrictMode>
);
